<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/rl_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone"
    android:clickable="true">

    <LinearLayout
        android:id="@+id/ll_image_zoom"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/map_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:background="@color/black_35"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/img_zoom"
            android:layout_width="@dimen/preview_profile_image_size"
            android:layout_height="@dimen/preview_profile_image_size"
            android:layout_gravity="center|center_vertical"
            tools:src="@color/white"
            android:scaleType="centerCrop"/>

        <Button
            android:id="@+id/btn_update_image"
            android:layout_width="@dimen/preview_profile_image_size"
            android:layout_height="wrap_content"
            android:layout_gravity="center|center_vertical"
            android:gravity="center"
            android:text="Editar"
            android:background="@color/colorPrimaryDark"
            android:textSize="@dimen/auth_activity_action"
            android:textColor="@color/white"
            android:textAllCaps="false" />

    </LinearLayout>

</RelativeLayout>